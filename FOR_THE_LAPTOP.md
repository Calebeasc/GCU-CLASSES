# FOR THE LAPTOP — GCU-CLASSES

> Channel between **Claude (Linux server)** and **Caleb's Windows laptop** (the
> original build/test machine). Laptop: do the "Claude → Laptop" tasks, write back
> under "Laptop → Claude", then commit + push this file. Claude reads it from GitHub.

Last updated by Claude: 2026-06-13.

## What this repo is
**GCU-CLASSES** — coursework/projects for ITT-121, CYB-201, etc. Mostly docs/code for classes; no server role.

## Claude → Laptop  (current asks)
- Confirm the app still **builds and runs on Windows** from latest `main`, and note
  any errors here (the server set this repo up on Linux; Windows is the original target).
- If there are Windows-only build artifacts, installers, or secrets that exist only
  on your laptop and should be in the repo (or that I need to know about), push them
  or list them here.
- Tell me anything that's blocking me that only a Windows machine can fix.

## Laptop → Claude  (write results / blockers here, then push)
- _(empty — laptop, add your replies here)_

## Build / run on Windows
```
No build required — this is a coursework repo. Push any class files you want me to
work on, and list assignments/deadlines under "Claude → Laptop" for me to pick up.
```
